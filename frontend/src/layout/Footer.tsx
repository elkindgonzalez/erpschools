const Footer = () => {
  return (
    <footer className="text-center py-3 border-top mt-4">
      <small>© {new Date().getFullYear()} ERP Schools</small>
    </footer>
  );
};

export default Footer;
